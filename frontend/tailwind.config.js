// Import the necessary plugin
import animatePlugin from 'tailwindcss-animate';

// TailwindCSS configuration with ES module syntax
export default {
    // Enabling dark mode with class strategy
    darkMode: 'class',
    // Specifying the content paths for Tailwind to purge unused styles in production
    content: [
        "./index.html", 
        "./src/**/*.{js,ts,jsx,tsx}",
        "./node_modules/@shadcn/ui/dist/**/*.js"
    ],
    // Extending the default theme
    theme: {
        extend: {
            // Custom fonts
            fontFamily: {
                billabong: ["Billabong", "cursive"]
            },
            // Custom grid template columns
            gridTemplateColumns: {
                '70/30': '70% 28%'
            },
            // Custom border radius values using CSS variables
            borderRadius: {
                lg: 'var(--radius)',
                md: 'calc(var(--radius) - 2px)',
                sm: 'calc(var(--radius) - 4px)'
            },
            // Colors defined with CSS variables for easier theming
            colors: {
                background: 'hsl(var(--background))',
                foreground: 'hsl(var(--foreground))',
                card: {
                    DEFAULT: 'hsl(var(--card))',
                    foreground: 'hsl(var(--card-foreground))'
                },
                popover: {
                    DEFAULT: 'hsl(var(--popover))',
                    foreground: 'hsl(var(--popover-foreground))'
                },
                primary: {
                    DEFAULT: 'hsl(var(--primary))',
                    foreground: 'hsl(var(--primary-foreground))'
                },
                secondary: {
                    DEFAULT: 'hsl(var(--secondary))',
                    foreground: 'hsl(var(--secondary-foreground))'
                },
                muted: {
                    DEFAULT: 'hsl(var(--muted))',
                    foreground: 'hsl(var(--muted-foreground))'
                },
                accent: {
                    DEFAULT: 'hsl(var(--accent))',
                    foreground: 'hsl(var(--accent-foreground))'
                },
                destructive: {
                    DEFAULT: 'hsl(var(--destructive))',
                    foreground: 'hsl(var(--destructive-foreground))'
                },
                border: 'hsl(var(--border))',
                input: 'hsl(var(--input))',
                ring: 'hsl(var(--ring))',
                chart: {
                    '1': 'hsl(var(--chart-1))',
                    '2': 'hsl(var(--chart-2))',
                    '3': 'hsl(var(--chart-3))',
                    '4': 'hsl(var(--chart-4))',
                    '5': 'hsl(var(--chart-5))'
                }
            }
        }
    },
    // Registering the plugin
    plugins: [animatePlugin]
};
